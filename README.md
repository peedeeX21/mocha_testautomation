A demo setup for mocha tests automation using karma.

Mocha tests are usinc chai.js & chai-as-promised.js as assertion language.

## Set up:

install: `npm install`

## Run tests:

run tests: `npm test`
